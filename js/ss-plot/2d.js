function FlatPlot( params )
{
  var __canvas = params.canvas,

  _leftEdge = params.leftEdge,

  _width = params.width,

  _height = params.height,

  _hTics = params.hTics,

  _vTics = params.vTics,

  plotAxes = function()
  {
  },

  flatPlot = {};


  return flatPlot;
}
